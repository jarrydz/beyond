node_modules
dist
dist-ssr
*.local
.vite
.DS_Store
tsconfig.tsbuildinfo

# Local env files — never commit secrets (e.g. future Supabase keys).
# Templates like .env.example stay tracked.
.env
.env.*
!.env.example


# Local full-res source for the home background (optimised copy lives in public/)
beyond-bg.original.jpg
